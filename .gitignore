# ============================================================================
# scholar-md .gitignore
#
# 公开(进 git → GitHub):scripts/ 工具代码 + 开源标配
#   (README / LICENSE / DISCLAIMER / requirements.txt / .gitignore)。
# 私有(不公开,留本地/网盘):
#   - 01_System/   规程与 SOP
#   - AGENTS.md    agent 行为宪法
#   - 02_Source/   输入 PDF        03_Output/  转换产物        04_Docs/  本地笔记
# 数据通过 OneDrive/坚果云等网盘在多台设备间同步;公开仓库只含工具与开源标配。
# ============================================================================

# --- Python 环境与缓存 ---
.venv/
.venv-textbooks/
__pycache__/
*.pyc
*.pyo

# --- OCR 语言数据(纯数据文件,~15MB;来源 github.com/tesseract-ocr/tessdata_best,
#     provenance JSON 记录其 sha256;ocr_layer.py 默认读此处) ---
.tessdata/

# --- IDE / 本地设置 ---
.claude/
.vscode/

# --- 私有:规程与 agent 规则(不公开) ---
01_System/
AGENTS.md
TODO.md

# --- 私有:输入 / 产物 / 笔记数据(走网盘,不公开) ---
02_Source/
03_Output/
04_Docs/

# --- 私有:调试视图产物(内嵌文档页面图;所有者指定默认落 scripts/pipelines/patents/,
#     故全局拦截,任何位置都不入 git) ---
*_debug.html
*_annotations.json

# --- 本地手工测试临时产物(patents 端到端 smoke 的输入 PDF + 页面 PNG,二进制,不入 git) ---
_selftest/

# --- pytest 本地 basetemp(本机系统临时目录 ACL 拒绝写入,--basetemp=./.pytest_tmp 兜底,
#     见 lessons L-T22;纯测试夹具产物,含 tmp_path 生成的假 PDF/检查点,不入 git) ---
.pytest_tmp/
