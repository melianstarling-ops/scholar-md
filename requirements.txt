# scholar-md — patents 管线依赖
#
# 核心:专利几何解析只依赖 PyMuPDF(提供 `fitz`)。
# 可选:Tier1 云端 AI 审查(ai_review.py)按需安装对应 SDK,无 API key 时不需要。

# --- 核心(必需) ---
# 1.24 系列末版:与原 1.24.10 同系、API 零漂移，且带 Python 3.13 wheel
# （1.24.10 在 3.13 上无 wheel、需编译 VS）。实测全流程 Tier0 0 丢失、各修复一致。
PyMuPDF==1.24.14

# --- 可选:Tier1 AI 审查(按你用的模型供应商选装) ---
# anthropic==0.46.0
# openai==1.109.1
# google-genai==1.74.0
