# scholar-md — patents 管线依赖
#
# 核心:专利几何解析只依赖 PyMuPDF(提供 `fitz`)。
# 可选:Tier1 云端 AI 审查(ai_review.py)按需安装对应 SDK,无 API key 时不需要。

# --- 核心(必需) ---
# 升到 1.27.2.3：为将来评估迁移到 PyMuPDF4LLM 铺路(两者版本锁步)。Python 3.10–3.14
# 均有 wheel；本仓用 Python 3.13。实测全流程 Tier0 0 丢失、各修复与 1.24.x 一致
# (核心 get_text/get_pixmap API 在 1.24↔1.27 间未变)。
PyMuPDF==1.27.2.3

# --- 交叉校验(crosscheck_words.py:extract_words 第二取词器,离线审计) ---
# 迁移评估结论(04_Docs/pymupdf4llm-迁移评估.md):规则不迁移,仅用作独立校验源。
pymupdf4llm==1.27.2.3

# --- 可选:Tier1 AI 审查(按你用的模型供应商选装) ---
# anthropic==0.46.0
# openai==1.109.1
# google-genai==1.74.0
